---
import '../styles/fonts.css';
import '../styles/site.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { alternatePath, type Locale } from '../data/site';
const { lang = 'en', title = 'Caelis Labs', description, noindex = false, compactFooter = false } = Astro.props as { lang?: Locale; title?: string; description: string; noindex?: boolean; compactFooter?: boolean };
const canonical = new URL(Astro.url.pathname, Astro.site);
const pageTitle = title === 'Caelis Labs' ? 'Caelis Labs — Open tools. Connected intelligence.' : `${title} · Caelis Labs`;
---
<!doctype html>
<html lang={lang === 'en' ? 'en' : 'zh-CN'} data-theme="light">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>{lang === 'zh-cn' && title === 'Caelis Labs' ? 'Caelis Labs — 开放的工具，协作的智能。' : pageTitle}</title>
    <meta name="description" content={description}/>
    <meta name="theme-color" content="#0b0c12" media="(prefers-color-scheme: dark)"/>
    <meta name="theme-color" content="#fafaf8" media="(prefers-color-scheme: light)"/>
    <meta property="og:type" content="website"/><meta property="og:site_name" content="Caelis Labs"/>
    <meta property="og:title" content={title}/><meta property="og:description" content={description}/><meta property="og:url" content={canonical}/>
    <meta property="og:locale" content={lang === 'en' ? 'en_US' : 'zh_CN'}/>
    <meta name="twitter:card" content="summary_large_image"/>
    <meta name="twitter:title" content={title}/><meta name="twitter:description" content={description}/>
    {!noindex && <>
      <link rel="canonical" href={canonical}/>
      <link rel="alternate" hreflang={lang === 'en' ? 'en' : 'zh-CN'} href={canonical}/>
      <link rel="alternate" hreflang={lang === 'en' ? 'zh-CN' : 'en'} href={new URL(alternatePath(Astro.url.pathname), Astro.site)}/>
      <link rel="alternate" hreflang="x-default" href={new URL(Astro.url.pathname.replace(/^\/zh-cn(?=\/)/, ''), Astro.site)}/>
    </>}
    {noindex && <meta name="robots" content="noindex"/>}
    <link rel="icon" href="/icon.svg" type="image/svg+xml"/>
    <link rel="icon" href="/favicon-32.png" sizes="32x32"/>
    <link rel="apple-touch-icon" href="/apple-touch-icon.png"/>
    <script is:inline src="/theme.js"></script>
  </head>
  <body>
    <Header lang={lang}/>
    <main id="main"><slot/></main>
    <Footer lang={lang} compact={compactFooter}/>
    <script>import '../scripts/site';</script>
  </body>
</html>
